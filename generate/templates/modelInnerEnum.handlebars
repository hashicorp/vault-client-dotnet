        {{#unless isContainer}}/// <summary>
        /// {{#with description}}{{.}}{{else}}Defines {{{name}}}{{/with}}
        /// </summary>
        {{#with description}}/// <value>{{.}}</value>{{/with}}
        {{#if isString}}[JsonConverter(typeof(StringEnumConverter))]{{/if}}
        {{>visibility}} enum {{datatypeWithEnum}}{{#unless datatypeWithEnum}}{{classname}}{{/unless}}{{#with vendorExtensions.x-enum-byte}}: byte{{/with}}
        {
            {{#with allowableValues}}{{#each enumVars}}
            /// <summary>
            /// Enum {{name}} for value: {{value}}
            /// </summary>
            {{#if isString}}[EnumMember(Value = "{{{value}}}")]{{/if}}
            {{name}}{{#unless isString}} = {{{value}}}{{/unless}}{{#if isString}} = {{@index}}{{/if}}{{#unless @last}},{{/unless}}{{/each}}{{/with}}
        }{{/unless}}
